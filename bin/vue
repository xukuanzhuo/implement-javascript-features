#!/usr/bin/env node

require('commander')
  .version(require('../package').version, '-v, --version')
  .description('quick generate smile admin module:resouce views && api && store files')
  .usage('<command> [options]')
  .command('generate', 'generate smile/admin module:resource files')
  .alias('g')
  .parse(process.argv)
