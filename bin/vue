#!/usr/bin/env node

require('commander')
  .version(require('../package').version, '-v, --version')
  .description('quick generate vue file')
  .usage('<command> [options]')
  .command('generate', 'generate one new vue resource files')
  .alias('g')
  .parse(process.argv)
